# Vulnerable DNS configuration for IoT testbed
# VULN: No access control — responds to all queries
# VULN: Exposes internal hostnames and IPs

# Listen on all interfaces
listen-address=0.0.0.0
bind-interfaces

# Serve from custom hosts file (leaks internal topology)
no-hosts
addn-hosts=/etc/hosts.iot

# VULN: Enable DNS rebinding (no protection)
# VULN: Log all queries (information disclosure via timing)
log-queries
log-facility=-

# VULN: No DNSSEC validation
no-dnssec-check

# Cache poisoning: large cache with no validation
cache-size=1000

# VULN: Serve ANY record type
