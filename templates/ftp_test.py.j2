import ftplib

def test_FTP_ANON_LOGIN():
    ip = "{{ v.ip }}"
    port = int("{{ v.ports }}".split(",")[0])

    ftp = ftplib.FTP()
    ftp.connect(ip, port, timeout=5)
    resp = ftp.login()
    ftp.quit()

    assert "230" in resp
